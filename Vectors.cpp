#include<bits/stdc++.h>
// iostream is a very basic library in C++ for vectors we will have to use the library  <bits/stde++h>
using namespace std;
int main()
{
    vector<int>arr2;
    // A vector which contains integer elements called arr2
    arr2.push_back(1);
    // push 1 to the 1 st element of the vector 
    cout<<arr2[0]<<endl;
    //print the oth element of arr2
    arr2.push_back(2);
    cout<<arr2[1]<<endl;
    return 0;

}