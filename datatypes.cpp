#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
    int a;
    a = INT_MAX ;
    cout << a << endl;
    // You can view the ouput as the maximum possible integer i.e the Biggest integer the datatype can store
    cout << "this is likith\n";
    cout << "Check the spaces in between";
    // We can use \t or \n to get the next output printed after 4 spaces and next line respectively
    cout<< "This is Likith,\b";
    cout<< "Check the use of back space" << endl;
    // Char is used to store a letter in the form ''
    cout << char(97) << endl;
    // We tell the computer to print the character with ASCII value 97
    // Refer the link https://www.programiz.com/cpp-programming/data-types for more understanding and limits of the datatype.
    int i ;
    i = 3.14448449949499 ;
    cout << fixed << setprecision(1) << i << endl;
    // The 1 denotes no of integers (0-9) we wish to see in the output
    // While using setprecision we will have to use advanced libraries like bits/stdc++.h and iomainp ios

}
